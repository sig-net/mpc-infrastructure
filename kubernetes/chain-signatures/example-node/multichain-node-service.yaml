apiVersion: v1
kind: Service
metadata:
  name: multichain-node
  namespace: chain-sig
  annotations:
    cloud.google.com/l4-rbs: "enabled"
spec:
  type: LoadBalancer
  selector:
    app: multichain-node
  ports:
    - name: tcp-port
      protocol: TCP
      port: 3000
      targetPort: 3000
