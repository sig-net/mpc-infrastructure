apiVersion: v1
kind: Service
metadata:
  name: multichain-testnet-8
  namespace: chain-sig
  annotations:
    cloud.google.com/l4-rbs: "enabled"
spec:
  type: LoadBalancer
  selector:
    app: multichain-8
  ports:
    - name: tcp-port
      protocol: TCP
      port: 3000
      targetPort: 3000
